#include <IoSdMeteo.h>
#include <sstream>
#include<iostream>
#include<fstream>

#if 0
class IoSdMeteoData {
public:
typedef enum {eTemperature, eHumidity, eWindSpeed, eWindSpeedInst, eWindSpeedMax, eWindDirection, ePressure, eLastMeteoData} IoSdMeteoDataEnum;
double d[eLastMeteoData];

ClassDef(IoSdMeteoData, 1)
};


#endif
